name: bestie_windows
description: MyTaskKing — Windows desktop workspace.
version: 1.0.0+1
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  bestie_design:
    path: ../shared_design_system
  bestie_core:
    path: ../shared_core
  bestie_mobile:
    path: ../mobile_app
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.1
  window_manager: ^0.4.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
